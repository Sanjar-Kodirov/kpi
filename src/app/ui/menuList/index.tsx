import React, { FC } from "react";

// const recursive = (item: any) => {
//   if (item.children.length) {
//     return item.children.map((itemChild) => <CatalogMenuItem name={itemChild.name} />);
//   }
// };

export const MenuCatalog: FC = () => {
  return <div></div>;
};
